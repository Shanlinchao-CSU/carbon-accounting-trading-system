import { createStore } from "vuex";

export default createStore({
    state: {
        can_click_button:true,
        is_login:false,
        avatar:"",
    },
    getters: {},
    mutations: {},
    actions: {},
    modules: {},
});
